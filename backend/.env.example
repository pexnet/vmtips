# Public account creation is disabled for the initial release.
ALLOW_PUBLIC_REGISTRATION=false

# Private JSON file containing initial usernames and passwords.
# Local default: ../data/start_users.json. Docker production default: /data/start_users.json.
# START_USERS_FILE=/data/start_users.json
